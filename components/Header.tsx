"use client";
import Link from "next/link";import {Menu,X} from "lucide-react";import {useEffect,useRef,useState} from "react";import {site} from "@/lib/site";
export function Header(){const [open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false);if(e.key==="Tab"&&open){const items=ref.current?.querySelectorAll<HTMLElement>("a,button");if(items?.length&&e.shiftKey&&document.activeElement===items[0]){e.preventDefault();items[items.length-1].focus()}else if(items?.length&&!e.shiftKey&&document.activeElement===items[items.length-1]){e.preventDefault();items[0].focus()}}};document.addEventListener("keydown",key);return()=>document.removeEventListener("keydown",key)},[open]);return <header className="nav"><div className="shell navin"><Link className="brand" href="/" aria-label="Telamon Consulting home">TELAMON<small>CONSULTING</small></Link><nav className="links" aria-label="Primary">{site.nav.map(n=><Link key={n.href+n.label} href={n.href}>{n.label}</Link>)}</nav><Link className="button gold" href="/contact">Book a Discovery Call</Link><button className="menu" aria-expanded={open} aria-controls="mobile-nav" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}<span className="skip">Menu</span></button></div>{open&&<div id="mobile-nav" className="mobilePanel" ref={ref} role="dialog" aria-label="Mobile navigation">{site.nav.map(n=><Link key={n.href+n.label} href={n.href} onClick={()=>setOpen(false)}>{n.label}</Link>)}<Link className="button gold" href="/contact" onClick={()=>setOpen(false)}>Book a Discovery Call</Link></div>}</header>}
