import type {MetadataRoute} from "next";import {site} from "@/lib/site";export default function sitemap():MetadataRoute.Sitemap{const paths=["","/services","/services/automation-ai","/services/ai-adoption-governance","/services/information-management","/services/business-systems","/services/data-reporting","/services/security","/about","/contact","/privacy","/cookies"];return paths.map(path=>({url:`${site.url}${path}`,lastModified:new Date()}))}
