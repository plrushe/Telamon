import type {Metadata} from "next";import Link from "next/link";import {ArrowRight} from "lucide-react";import {site} from "@/lib/site";import {serviceIconMap} from "@/lib/icons";import {Breadcrumb} from "@/components/Breadcrumb";
export const metadata:Metadata={title:"Services",description:"Microsoft automation, AI, data and security services, each scoped around a measurable business outcome."};
export default function Services(){return <><section className="dark pageHero"><div className="shell"><Breadcrumb items={[{label:"Home",href:"/"},{label:"Services"}]}/><p className="eyebrow">TELAMON SERVICES</p><h1>Technology that carries more of the load.</h1><p className="lead">Six practical services on the Microsoft platform you already own, each scoped around a measurable business outcome — not activity for its own sake.</p><Link className="button gold" href="/contact">Book a Discovery Call <ArrowRight size={16}/></Link></div></section><section className="section services light"><div className="shell"><div className="grid3">{site.services.map((s,i)=>{const I=serviceIconMap[s.icon];return <article className="card" key={s.href}><span className="num">0{i+1}</span><span className="iconWrap"><I/></span><h3>{s.title}</h3><p>{s.description}</p><p className="auditMeta"><strong>Best for:</strong> {s.bestFor}</p><Link className="serviceLink" href={s.href}>Explore service <ArrowRight size={14}/></Link></article>})}</div></div></section><section className="section dark" id="audits"><div className="shell"><div className="sectionHead"><p className="eyebrow">STARTING-POINT AUDITS</p><h2>Not ready for a full transformation? Start smaller.</h2></div><div className="grid3">{site.audits.map(a=><article className="card" key={a.title}><h3>{a.title}</h3><p><strong>Recommended for:</strong><br/>{a.forWho}</p><p style={{marginTop:14}}><strong>What you get:</strong><br/>{a.receives}</p><p className="auditMeta">{a.format}</p><Link className="serviceLink" href="/contact">Discuss this audit <ArrowRight size={14}/></Link></article>)}</div></div></section><section className="cta"><div className="shell ctaIn"><div><h2>Not sure which package fits?</h2><p>Book a strategy session and we will help identify the technical roadmap that prioritises your business growth and streamlines your daily operations.</p></div><div className="actions"><Link className="button gold" href="/contact">Book a Discovery Call</Link></div></div></section></>}
