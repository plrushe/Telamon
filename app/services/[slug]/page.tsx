import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {ArrowRight,CheckCircle2} from "lucide-react";import {site} from "@/lib/site";import {serviceIconMap} from "@/lib/icons";import {Breadcrumb} from "@/components/Breadcrumb";
function findService(slug:string){return site.services.find(s=>s.href===`/services/${slug}`)}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=findService(slug);if(!service)notFound();const I=serviceIconMap[service.icon];return <><section className="dark pageHero"><div className="shell"><Breadcrumb items={[{label:"Home",href:"/"},{label:"Services",href:"/services"},{label:service.title}]}/><p className="eyebrow">SERVICE</p><span className="iconWrap" style={{marginBottom:20}}><I color="var(--light-gold)" size={24}/></span><h1>{service.title}</h1><p className="lead">{service.description}</p><div className="actions"><Link className="button gold" href="/contact">Book a Discovery Call <ArrowRight size={16}/></Link><Link className="button outline" href="/services">All services</Link></div><div className="bestForBox"><strong>BEST FOR</strong><span>{service.bestFor}</span></div></div></section><section className="section light"><div className="shell problemGrid"><div><p className="eyebrow">THE CHALLENGE</p><h3>Where this starts</h3><p style={{color:"#56564e"}}>{service.problem}</p></div><div><p className="eyebrow">OUR APPROACH</p><h3>What we do</h3><p style={{color:"#56564e"}}>{service.approach}</p></div></div></section><section className="section dark"><div className="shell"><div className="sectionHead"><p className="eyebrow">WHAT YOU GET</p><h2>Delivered as a documented, handed-over asset.</h2></div><div className="checklist">{service.deliverables.map(d=><div className="checkItem" key={d}><CheckCircle2/><p>{d}</p></div>)}</div></div></section><section className="cta"><div className="shell ctaIn"><div><h2>Ready to talk about {service.title}?</h2><p>Start with a focused conversation about where this could reduce manual work or risk in your organisation.</p></div><div className="actions"><Link className="button gold" href="/contact">Book a Discovery Call</Link><Link className="button outline" href="/services">Explore other services</Link></div></div></section></>}
export function generateStaticParams(){return site.services.map(s=>({slug:s.href.split("/").pop() as string}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const service=findService(slug);return {title:service?.title??site.name,description:service?.description}}
